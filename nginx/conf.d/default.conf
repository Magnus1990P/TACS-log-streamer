# Additional nginx configuration for OAuth2 Log Streamer
# This file is mounted to /etc/nginx/conf.d/default.conf

# Server block for additional configurations
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 404;
}
